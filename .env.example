# Copy this file to .env and fill the values. Do NOT commit your real keys.
SENDGRID_API_KEY=your_sendgrid_api_key_here
FIREBASE_SERVICE_ACCOUNT=path/to/firebase-service-account.json
FROM_EMAIL=noreply@example.com
CONTINUE_URL=https://example.com/reset-password

# Optional SMTP settings (use these if you want to send via your mail server instead of SendGrid)
SMTP_HOST=smtp0001.neo.space
SMTP_PORT=587
SMTP_USER=animeshkukreti@gatekeeper.sbs
SMTP_PASS=archisismybrother
SMTP_USE_TLS=true
SMTP_SSL=false
FROM_EMAIL=animeshkukreti@gatekeeper.sbs
